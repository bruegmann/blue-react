import { ReactNode } from "react";
export interface BsModalFooterProps {
    className?: string;
    children?: ReactNode;
}
export default function BsModalFooter({ className, children }: BsModalFooterProps): JSX.Element;
